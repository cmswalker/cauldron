function chart_ctrl() {
	

	// var ChartApp = angular.module("ChartApp", []);

	// ChartApp.config(["$httpProvider", function($httpProvider) {
	//     $httpProvider.
	//         defaults.headers.common["X-CSRF"] = $("meta[name=csrf-token]").attr("content");
	// }]);

	// ChartApp.config(function($routeProvider) {

	// 	$routeProvider
	// 		.when('/',{
	// 			templateUrl: '/index.html',
	// 			controller: 'MainCtrl'
	// 		})
	// 		.when("/charts/id", {
	// 			controller: "ChartCtrl"
	// 		});

	// });



	// var ChartApp = angular.module("ChartApp", []);

	// ChartApp.config(["$httpProvider", function($httpProvider) {
	//     $httpProvider.
	//         defaults.headers.common["X-CSRF"] = $("meta[name=csrf-token]").attr("content");
	// }]);

	// ChartApp.controller("ChartCtrl", function ($scope, $http) {


	// });






}